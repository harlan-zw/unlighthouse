/**
 * Back-compat shim — the icon system now lives in `./icons`.
 * See `./icons/registry.ts` for the role vocabulary and `./icons/index.ts` for
 * the resolver + active-set switch.
 */
export * from './icons'
