/**
 * Target registry — import your target configs and export them as an array.
 *
 * Setup:
 *   1. Create target config files (see example.config.ts)
 *   2. Copy this file → registry.ts
 *   3. Uncomment / add your imports below
 *   4. Run:  npx playwright test
 *
 * This file (example.registry.ts) is tracked in git as a template.
 * Your actual registry.ts is gitignored — it's personal to your setup.
 */

// import mySite from './my-site.config.js';
// import anotherSite from './another-site.config.js';

// export const targets = [mySite, anotherSite];

// Placeholder — replace with your targets
export const targets: never[] = [];
