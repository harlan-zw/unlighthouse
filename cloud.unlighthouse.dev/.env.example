# Chrome Pool Configuration (for /api/scan - self-hosted)
# Uncomment to configure self-hosted Chrome instances
# NUXT_LIGHTHOUSE_MIN_CHROME_INSTANCES=1
# NUXT_LIGHTHOUSE_MAX_CHROME_INSTANCES=5
# NUXT_LIGHTHOUSE_CHROME_IDLE_TIMEOUT=300000
# NUXT_LIGHTHOUSE_MAX_CONCURRENCY=3

# Browserless Configuration (for /api/scan-browserless - managed service)
# Required for Browserless integration
# Get your token from https://browserless.io
NUXT_BROWSERLESS_TOKEN=your-token-here

# Optional: Self-hosted Browserless instance
# NUXT_BROWSERLESS_URL=https://chrome.browserless.io

# Database Configuration
# DATABASE_DIR=./data  # Directory for SQLite database (default: ./data)
# DATABASE_URL=./data/lighthouse.db  # Full path to database file (optional)
